- link:
    ~/.nvimrc: neovimrc
    ~/.bashrc: bashrc
    ~/.gitconfig: gitconfig
    ~/.gitignore: gitignore

- shell:
    -
        - "git submodule update --init --recursive --remote"
        - "Installing/Updating submodules"
