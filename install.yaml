- link:
    ~/.nvimrc: neovimrc
    ~/.bashrc: bashrc
    ~/.profile: profile
    ~/.gitconfig: gitconfig
    ~/.gitignore: gitignore

- shell:
    -
        - "git submodule update --init --recursive --remote"
        - "Installing/Updating submodules"
